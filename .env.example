# Google AI (Gemini) API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# AI Model Configuration
# Recommended models:
# - gemini-2.5-flash-image-preview (Latest, best for images)
# - gemini-2.0-flash-exp (Experimental, fast)
# - gemini-1.5-pro (More accurate, slower)
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image-preview

# Optional: Gemini API Endpoint (usually not needed)
# GEMINI_API_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated origins, or * for all)
ALLOWED_ORIGINS=*
