# =============================================================================
# AgriVision MCP Server - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# AI Vision Model Configuration
# -----------------------------------------------------------------------------
# Provider: Google Gemini AI
# Current Model: gemini-2.5-flash-image-preview
#
# Supported Models:
# - gemini-2.5-flash-image-preview (Recommended: Latest, optimized for images)
# - gemini-2.0-flash-exp (Experimental: Fastest, good accuracy)
# - gemini-1.5-pro (Most accurate: Slower, best for complex cases)
# - gemini-1.5-flash (Balanced: Fast and accurate)
#
# Get API Key: https://makersuite.google.com/app/apikey
# -----------------------------------------------------------------------------
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image-preview

# Optional: Custom Gemini API Endpoint (leave blank for default)
# GEMINI_API_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/

# -----------------------------------------------------------------------------
# Diagnosis Mode Configuration
# -----------------------------------------------------------------------------
# Controls whether AI generates treatment recommendations or returns diagnosis only
#
# ADVISORY_MODE options:
#   - "diagnosis_only" (default): Returns structured diagnostic data only
#       - Crop identification, disease detection, pest identification
#       - Scientific names, severity levels, affected plant parts
#       - Client-side agent generates region-specific treatment advice
#       - Best for: Multi-region deployments, custom treatment logic
#
#   - "full_advisory": Returns diagnosis + AI-generated treatment recommendations
#       - Includes all diagnostic data PLUS treatment suggestions
#       - Organic and chemical treatment options
#       - Application methods and preventive measures
#       - Best for: Single-region deployments, standalone use
#
# Default: diagnosis_only (recommended for maximum flexibility)
# -----------------------------------------------------------------------------
ADVISORY_MODE=diagnosis_only

# -----------------------------------------------------------------------------
# Output Format Configuration
# -----------------------------------------------------------------------------
# Controls whether response is structured JSON or formatted text
#
# OUTPUT_FORMAT options:
#   - "structured" (default): Returns JSON with typed fields
#       - Easier to parse programmatically
#       - Consistent schema for client applications
#       - Best for: API integrations, structured data processing
#
#   - "text": Returns human-readable formatted text
#       - Easier to read directly
#       - Includes formatting (emojis, headers, bullets)
#       - Best for: Direct display to end users, testing
#
# Default: structured (recommended for production)
# -----------------------------------------------------------------------------
OUTPUT_FORMAT=structured

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated origins, or * for all)
ALLOWED_ORIGINS=*
